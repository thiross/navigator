apply plugin: 'groovy'
apply plugin: 'java-gradle-plugin'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile gradleApi()
    compile localGroovy()
    compile project(':navigator-const')
    compile deps.android.plugin
}

