apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile deps.tutuur.const
    compile deps.android.plugin
}

apply from: '../upload-maven.gradle'
